# tainacan_get_svg_allowed_html


Returns SVG allowed HTML elements and attributes for wp_kses

***

* Full name: `tainacan_get_svg_allowed_html`
* Defined in: `classes/tainacan-utils.php`

## Parameters

This function has no parameters.

## Return Value

**array**

Array of SVG-related HTML elements and their allowed attributes
